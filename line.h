#include <stdio.h>
#include <iostream> 
#include <list>
#include <block.h>

class Line {
	private:
		int n_set;
		list <block> n_blocks;		
	public:
		Line (int sets);
		set_block ()
		//block GetBlock(int set_num);
		
};
